{
  "framework": "nextjs",
  "rootDirectory": "apps/web",
  "buildCommand": "cd ../.. && pnpm turbo run build --filter=@asa/web...",
  "outputDirectory": ".next",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "env": {
    "NEXTAUTH_URL": "@nextauth_url"
  }
}
